public class IntNode {
	//fields of the object
	int data; //data part of node
	IntNode next; //link to next node on linked list
	
	
	IntNode (int data, IntNode next){
		this.data = data;
		this.next = next;
		//constructor, creates new object, intiziled object
	}
}